import Slides from "../slides";

let config = {
  reveal: {
    controls: false,
    height: 720,
    help: false,
    history: true,
    keyboard: false,
    overview: false,
    touch: false,
    transition: "convex",
    width: 1280,
  },
  slides: Slides,
};

export default config;
