import Slides from "../slides";

let config = {
  reveal: {
    controls: true,
    height: 720,
    help: false,
    history: true,
    keyboard: true,
    progress: false,
    slideNumber: true,
    touch: false,
    transition: "convex",
    width: 1280,
  },
  slides: Slides,
};

export default config;
