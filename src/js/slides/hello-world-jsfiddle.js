import Slide from "../framework/slide";

class HelloWorldJsFiddle extends Slide {
  constructor(config) {
    super(config);
    this.completed = false;
    this.init();
  }

  init() {}

  activate() {}
}

export default HelloWorldJsFiddle;
